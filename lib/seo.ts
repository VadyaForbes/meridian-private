import type {Metadata} from "next";import {locales,type Locale} from "./i18n";
export function pageMetadata(locale:Locale,path:string,title:string,description:string):Metadata{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://meridianprivate.com",suffix=path?`/${path}`:"",languages={...Object.fromEntries(locales.map(l=>[l,`${base}/${l}${suffix}`])),"x-default":`${base}/en${suffix}`};return{title,description,alternates:{canonical:`${base}/${locale}${suffix}`,languages},openGraph:{title,description,url:`${base}/${locale}${suffix}`,siteName:"Meridian Private",locale,type:"website",images:[{url:"/opengraph-image",width:1200,height:630}]},twitter:{card:"summary_large_image",title,description,images:["/opengraph-image"]}}}
