import type {Metadata} from "next";import {notFound} from "next/navigation";import {PageHero} from "@/components/page-hero";import {getDictionary,isLocale} from "@/lib/i18n";import {pageMetadata} from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return pageMetadata(locale,"privacy",d.legal.privacyTitle,d.legal.privacyIntro)}
export default async function Privacy({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <><PageHero eyebrow={d.footer.privacy} title={d.legal.privacyTitle} intro={d.legal.privacyIntro}/><section className="section"><div className="narrow">{d.legal.privacySections.map(s=><article className="prose-section" key={s[0]}><h2>{s[0]}</h2><p>{s[1]}</p></article>)}</div></section></>}
