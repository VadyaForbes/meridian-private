import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getDictionary,isLocale } from "@/lib/i18n";
import {pageMetadata} from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{locale:string;country:string}>}):Promise<Metadata>{const{locale,country}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);const item=d.destinations.countries[country as keyof typeof d.destinations.countries];return item?pageMetadata(locale,`destinations/${country}`,item[0],item[1]):{};}
export default async function Country({params}:{params:Promise<{locale:string;country:string}>}){const{locale,country}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale),item=d.destinations.countries[country as keyof typeof d.destinations.countries];if(!item)notFound();return <><header className="page-hero"><div className="shell"><Link href={`/${locale}/destinations`} className="eyebrow">← {d.common.back}</Link><h1 className="page-title">{item[0]}</h1><p className="lead">{item[1]}</p></div></header><section className="section"><div className="shell content-grid"><aside><p className="eyebrow">{d.common.eyebrow}</p><p className="notice">{d.common.noListings}</p></aside><div><article className="prose-section"><h2>{d.country.context}</h2><p>{d.country.body}</p></article><article className="prose-section"><h2>{d.country.questions}</h2><ul>{d.country.questionsList.map(x=><li key={x}>{x}</li>)}</ul></article><article className="prose-section"><h2>{d.country.support}</h2><ul>{d.country.supportList.map(x=><li key={x}>{x}</li>)}</ul></article><Link className="button" href={`/${locale}/buyer-brief`}>{d.common.start}</Link></div></div></section></>}
