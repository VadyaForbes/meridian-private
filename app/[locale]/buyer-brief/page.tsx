import type {Metadata} from "next";import {notFound} from "next/navigation";import {BuyerForm} from "@/components/buyer-form";import {getDictionary,isLocale} from "@/lib/i18n";import {pageMetadata} from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return pageMetadata(locale,"buyer-brief",d.brief.title,d.brief.intro)}
export default async function Brief({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <section className="section"><div className="shell form-layout"><aside className="form-side"><p className="eyebrow">{d.common.eyebrow}</p><h1 className="page-title">{d.brief.title}</h1><p className="lead">{d.brief.intro}</p><p className="notice">{d.contact.note}</p></aside><BuyerForm locale={locale}/></div></section>}
