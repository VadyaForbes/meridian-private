import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {PageHero} from "@/components/page-hero";import {getDictionary,isLocale} from "@/lib/i18n";import {pageMetadata} from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return pageMetadata(locale,"buying-process",d.nav.process,d.process.intro)}
export default async function Process({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <><PageHero eyebrow={d.common.eyebrow} title={d.process.title} intro={d.process.intro}/><section className="section"><div className="narrow">{d.process.steps.map(step=><article className="step" key={step[0]}><span className="step-num">{step[0]}</span><div><h2>{step[1]}</h2><p>{step[2]}</p></div></article>)}<div style={{marginTop:"3rem"}}><Link className="button" href={`/${locale}/buyer-brief`}>{d.common.start}</Link></div></div></section></>}
