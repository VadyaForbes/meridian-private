import type {Metadata} from "next";import {notFound} from "next/navigation";import {PageHero} from "@/components/page-hero";import {getDictionary,isLocale} from "@/lib/i18n";import {pageMetadata} from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return pageMetadata(locale,"about",d.nav.about,d.about.intro)}
export default async function About({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <><PageHero eyebrow={d.nav.about} title={d.about.title} intro={d.about.intro}/><section className="section"><div className="narrow">{d.about.sections.map((s,i)=><article className="prose-section" key={s[0]}><p className="eyebrow">0{i+1}</p><h2>{s[0]}</h2><p>{s[1]}</p></article>)}</div></section></>}
