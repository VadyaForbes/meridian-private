import type {Metadata} from "next";import {notFound} from "next/navigation";import {PageHero} from "@/components/page-hero";import {getDictionary,isLocale} from "@/lib/i18n";import {pageMetadata} from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return pageMetadata(locale,"terms",d.legal.termsTitle,d.legal.termsIntro)}
export default async function Terms({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <><PageHero eyebrow={d.footer.terms} title={d.legal.termsTitle} intro={d.legal.termsIntro}/><section className="section"><div className="narrow">{d.legal.termsSections.map(s=><article className="prose-section" key={s[0]}><h2>{s[0]}</h2><p>{s[1]}</p></article>)}</div></section></>}
