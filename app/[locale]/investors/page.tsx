import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {PageHero} from "@/components/page-hero";import {getDictionary,isLocale} from "@/lib/i18n";import {pageMetadata} from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return pageMetadata(locale,"investors",d.nav.investors,d.investors.intro)}
export default async function Investors({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <><PageHero eyebrow={d.nav.investors} title={d.investors.title} intro={d.investors.intro}/><section className="section"><div className="shell content-grid"><aside><p className="eyebrow">MANDATE / RISK / CONTEXT</p><p className="notice">{d.common.disclosure}</p></aside><div>{d.investors.sections.map((s,i)=><article className="prose-section" key={s[0]}><p className="eyebrow">0{i+1}</p><h2>{s[0]}</h2><p>{s[1]}</p></article>)}<Link className="button" href={`/${locale}/buyer-brief`}>{d.common.start}</Link></div></div></section></>}
