import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {PageHero} from "@/components/page-hero";import {getDictionary,isLocale} from "@/lib/i18n";import {pageMetadata} from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const{locale}=await params;if(!isLocale(locale))return{};const d=getDictionary(locale);return pageMetadata(locale,"contact",d.nav.contact,d.contact.intro)}
export default async function Contact({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <><PageHero eyebrow={d.nav.contact} title={d.contact.title} intro={d.contact.intro}/><section className="section"><div className="shell split"><div><p className="eyebrow">{d.contact.emailLabel}</p><p className="serif" style={{fontSize:"clamp(1.5rem,4vw,3rem)",overflowWrap:"anywhere"}}><a href={`mailto:${d.contact.email}`}>{d.contact.email}</a></p><hr className="rule"/><p className="eyebrow">{d.contact.hoursLabel}</p><p>{d.contact.hours}</p><p className="notice">{d.contact.note}</p></div><div className="quote-box"><h2 className="serif" style={{fontSize:"2.5rem",fontWeight:500}}>{d.brief.title}</h2><p>{d.brief.intro}</p><Link className="button" href={`/${locale}/buyer-brief`}>{d.common.start}</Link></div></div></section></>}
